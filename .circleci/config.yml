version: "2.1"
orbs:
  node: circleci/node@4.7.0
jobs: 
  build: 
    executor: node/default
    steps:
      - checkout
      - node/install-packages
      - run: npm run test
workflows:
  test_my_app:
  jobs:
    - test